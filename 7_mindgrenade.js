const num1=5;
const num2=10;

function addValues(){
    console.log(`the sum is : ${num1+num2}`);
}
addValues();
// here we are not exporting but we are exexuting the function so we dont  need to export 